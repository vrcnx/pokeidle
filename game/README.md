# Pokémon Idle

A Vite + React + TypeScript rebuild of pkmn-idle.com, recovered from the live bundle.

```bash
npm install
npm run dev      # http://localhost:5173
```

See [PROGRESS.md](PROGRESS.md) for what's implemented vs. what's stubbed.
